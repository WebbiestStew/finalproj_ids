PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret
DB_PATH=./data/dauto.db

# Comma-separated whitelist of allowed origins. Leave unset/empty for local
# development (falls back to open CORS) - only set this in production, e.g.:
# CORS_ORIGINS=https://dauto.example.com
CORS_ORIGINS=
